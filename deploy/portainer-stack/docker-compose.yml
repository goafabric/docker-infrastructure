version: "3"

services:
  portainer:
    image: portainer/portainer-ce
    ports:
    - "9000:9000"
    restart: always
#    command: --no-auth
    volumes:
    - portainer_data:/data
    - /var/run/docker.sock:/var/run/docker.sock
    networks:
    - portainer-net


volumes:
    portainer_data:

networks:
  portainer-net:
#    driver: bridge